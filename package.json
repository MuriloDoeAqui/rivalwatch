{
  "name": "rivalwatch-monorepo",
  "version": "1.0.0",
  "description": "RivalWatch - Price monitoring platform",
  "private": true,
  "engines": {
    "node": "24.x"
  },
  "scripts": {
    "dev": "cd frontend && npm install && npm run dev",
    "build": "cd frontend && npm install && npm run build",
    "worker": "cd worker && npm install && npm start",
    "check": "cd worker && npm install && npm run check"
  },
  "keywords": [
    "scraping",
    "price-monitor",
    "supabase",
    "react"
  ],
  "author": "",
  "license": "ISC"
}